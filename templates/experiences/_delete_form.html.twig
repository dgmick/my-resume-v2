<form class="my-5" method="post" action="{{ path('app_experiences_delete', {'id': experience.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ experience.id) }}">
    <button class="p-3 bg-color-red">Delete</button>
</form>
