<form class="my-5" method="post" action="{{ path('app_skill_chart_delete', {'id': skill_chart.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ skill_chart.id) }}">
    <button class="p-3 bg-color-red">Delete</button>
</form>
