<li class="flex items-center text-medium pb-5">
    <span class="mr-5">
        <i class="fas fa-envelope"></i>
    </span>
    <div>
        <p class="capitalize">{{ title }}</p>
        {% if title == 'email' %}
            <a class="text-color-light-gray" href="mailto:lutmickael@gmail.com">lutmickael@gmail.com</a>
        {% else %}
            <p class="text-color-light-gray">{{ value }}</p>
        {% endif %}
    </div>
</li>