{% extends 'base.html.twig' %}

{% block body %}
    {% if app.user %}
        <div class="mb-3 float-right">
            <a href="{{ path('app_logout') }}" class="bg-color-brown hover:bg-color-light-brown text-white font-bold py-2 px-4 rounded text-color-black-brown">Logout</a>
        </div>
    {% endif %}


    <div class="pt-10">
        {{ app.user.firstName }} {{ app.user.lastName }}
    </div>
{% endblock %}