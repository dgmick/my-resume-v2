<form class="my-5" method="post" action="{{ path('app_professional_skill_delete', {'id': professional_skill.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ professional_skill.id) }}">
    <button class="p-3 bg-color-red">Delete</button>
</form>
